FROM tomcat:9

CMD cp -r /usr/webapp/*.war /usr/local/tomcat/webapps/
RUN mkdir /usr/local/tomcat/ROOT/
CMD cp /usr/webapp/*/Config.properties /usr/local/tomcat/ROOT/Config.properties

EXPOSE 8080
CMD ["catalina.sh", "run"]

